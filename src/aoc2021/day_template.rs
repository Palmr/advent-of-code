///

pub fn solve_part_one(_input: &[String]) -> usize {
    0
}

#[test]
fn examples_part_one() {
    assert_eq!(
        1,
        solve_part_one(&[
            "VAL_1".to_string(),
            "VAL_2".to_string(),
        ])
    );
}

pub fn solve_part_two(_input: &[String]) -> usize {
    0
}

#[test]
fn examples_part_two() {
    assert_eq!(
        1,
        solve_part_two(&[
            "VAL_1".to_string(),
            "VAL_2".to_string(),
        ])
    );
}
